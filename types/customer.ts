export type Customer = {
  id: string;
  name: string;
  email: string;
  phone: string;
  point: number;
};

export type SimpleCustomer = {
  id: string;
  name: string;
  phone: string;
  point: number;
};
